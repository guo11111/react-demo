import React, {createContext, useContext} from "react";

function CounCom(props){
  console.log(props)
  // const CountCon = createContext();
  // let count = useContext(count)
  // console.log(count)
  return (<h1>{1}</h1>)
}

export default CounCom;